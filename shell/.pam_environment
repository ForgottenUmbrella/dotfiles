# Standard
HOME DEFAULT="/home/@{PAM_USER}"
PATH DEFAULT="${HOME}/.local/bin:${PATH}"
BROWSER DEFAULT="firefox"
TERMINAL DEFAULT="termite"
XDG_CONFIG_HOME DEFAULT="${HOME}/.config"
XDG_CACHE_HOME DEFAULT="${HOME}/.cache"
XDG_DATA_HOME DEFAULT="${HOME}/.local/share"
XDG_DATA_DIRS DEFAULT="/usr/local/share:/usr/share"
XDG_CONFIG_DIRS DEFAULT="/etc/xdg"
XDG_DESKTOP_DIR DEFAULT="${HOME}/Desktop"
XDG_DOCUMENTS_DIR DEFAULT="${HOME}/Documents"
XDG_DOWNLOAD_DIR DEFAULT="${HOME}/Downloads"
XDG_MUSIC_DIR DEFAULT="${HOME}/Music"
XDG_PICTURES_DIR DEFAULT="${HOME}/Pictures"
XDG_PUBLICSHARE_DIR DEFAULT="${HOME}/Public"
XDG_TEMPLATES_DIR DEFAULT="${HOME}/Templates"
XDG_VIDEOS_DIR DEFAULT="${HOME}/Videos"
XAUTHORITY DEFAULT="${XDG_RUNTIME_DIR}/Xauthority"
XINITRC DEFAULT="${XDG_CONFIG_HOME}/X11/xinitrc"
INPUTRC DEFAULT="${XDG_CONFIG_HOME}/readline/inputrc"
LESSHISTFILE DEFAULT="${XDG_CACHE_HOME}/less/history"
LESSKEY DEFAULT="${XDG_CONFIG_HOME}/less/lesskey"
LESS DEFAULT="-FRg"
SYSTEMD_LESS DEFAULT="FRXMK"
GNUPGHOME DEFAULT="${XDG_CONFIG_HOME}/gnupg"

# CLI
NPM_CONFIG_USERCONFIG DEFAULT="${XDG_CONFIG_HOME}/npm/npmrc"
TMUX_TMPDIR DEFAULT="${XDG_RUNTIME_DIR}"
ICEAUTHORITY DEFAULT="${XDG_RUNTIME_DIR}/ICEauthority"
_JAVA_OPTIONS DEFAULT="-Djava.util.prefs.userRoot=${XDG_CONFIG_HOME}/java"
PYLINTHOME DEFAULT="${XDG_CACHE_HOME}/pylint"
WINEPREFIX DEFAULT="${XDG_DATA_HOME}/wineprefixes/default"
FREETYPE_PROPERTIES DEFAULT="truetype:interpreter-version=35"
ANDROID_SDK_ROOT DEFAULT="/opt/android-sdk"
FZF_DEFAULT_COMMAND DEFAULT="find ."
GRADLE_USER_HOME DEFAULT="${XDG_DATA_HOME}/gradle"

# GUI
ANKI_BASE DEFAULT="${XDG_DATA_HOME}/anki"
GIMP2_DIRECTORY DEFAULT="${XDG_CONFIG_HOME}/gimp"
GTK_RC_FILES DEFAULT="${XDG_CONFIG_HOME}/gtk-1.0/gtkrc"
GTK2_RC_FILES DEFAULT="${XDG_CONFIG_HOME}/gtk-2.0/gtkrc"
QT_STYLE_OVERRIDE DEFAULT="kvantum"
VISUAL DEFAULT="emacsclient -c"
EDITOR DEFAULT="${VISUAL}"
#GIT_EDITOR DEFAULT="${EDITOR}"
SUDO_EDITOR DEFAULT="${EDITOR}"
ALTERNATE_EDITOR DEFAULT=""
GTK_IM_MODULE DEFAULT="ibus"
QT_IM_MODULE DEFAULT="ibus"
XMODIFIERS DEFAULT="@im=ibus"

# Temp
# Replace when libreoffice gets their shit together.
SAL_USE_VCLPLUGIN DEFAULT="gtk3"
