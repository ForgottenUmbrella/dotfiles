[Unit]
Description=A compositor for X11
Documentation=man:picom(1)
PartOf=graphical-session.target

[Service]
Type=simple
ExecStart=picom --experimental-backends

[Install]
WantedBy=i3-session.target
